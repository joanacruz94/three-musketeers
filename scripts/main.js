const menu = new Menu($canvas);

const snowFlakes = [];
/*
function drawObjects (){
    for (let i = 0; i < 500; i++) {
        snowFlakes.push(setRandomPosition(-2 * i));
    }
}


function setRandomPosition (randRow) {
    const row = randRow;
    const col = Math.floor(Math.random() * MAX_COL);
    const height = 35;
    const width = 35;
    return new SnowFlake(null, row, col, width, height, snowFlake);
}


function runLogic (){
    for(let z = 0; z < snowFlakes.length; z++)
        snowFlakes[z].runLogic();
}

function paint (){
    for(let z = 0; z < snowFlakes.length; z++)
        context.drawImage(snowFlake, snowFlakes[z].col * GRID_SIZE, snowFlakes[z].row * GRID_SIZE, snowFlakes[z].width, snowFlakes[z].height);

}
*/




